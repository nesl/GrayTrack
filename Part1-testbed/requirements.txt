# Python deps for the end-to-end reproduction pipeline (README steps 1–17).
# Install: pip install -r requirements.txt
#
# Not installed by this file (set up separately):
#   - carla (CARLA Python API / .egg matching your CARLA server)
#   - mininet / mn_wifi (Mininet-WiFi for step 3)
# System tools also required: ffmpeg, ffprobe, tshark, mergecap, capinfos, tcpdump

numpy==1.21.6
pandas==1.3.5
torch==1.13.1
tqdm==4.67.1
pyshark==0.6
matplotlib==3.5.3
